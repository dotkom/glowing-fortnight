name: Publish

on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Install dependencies
        run: yarn

      - name: Lint
        run: yarn lint

      - name: Compile
        run: yarn build

      - name: Test
        run: yarn test
